#ifndef EVENT_HPP
#define EVENT_HPP

/**
 * @brief Base class for events in the system.
 * 
 * This class serves as a generic event type that can be extended
 * to create specific event types for the event system.
 */
class Event {
  public:
   /**
     * @brief Default constructor.
     */
   Event() = default;

};
#endif